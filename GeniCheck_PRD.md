# GeniCheck PRD (백엔드 구현 기준)

> 원본: GeniCheck HR플랫폼 사업기획서 v2.0. AI 코딩 에이전트(Codex 등)에 컨텍스트로 투입할 목적의 PRD.
> 매출/시장분석 등 비개발 파트는 제외하고 구현 대상 기능/규칙만 정리함.

## 1. 제품 개요

- **제품명**: GeniCheck (지니체크)
- **한 줄 정의**: 지원자가 퇴직 후 이전 회사로부터 받은 역량 피드백(점수)을 자기 계정에 누적하고,
  다음 지원 회사에 자발적으로 제출하는 HR 레퍼런스 플랫폼.
- **핵심 설계 원칙**: 기업이 사람을 조회하는 구조가 아니라, **지원자가 데이터를 제출하는 구조**.
  이 방향성이 개인정보보호법/근로기준법/명예훼손 리스크를 회피하는 전제이므로, 모든 기능 설계에서
  이 원칙을 어기면 안 됨 (예: 기업이 임의로 지원자를 검색/조회하는 API는 존재해서는 안 됨).
- **핵심 액터**: `지원자(Applicant)`, `기업(Company)`, `시스템(자동 처리)`

---

## 2. 핵심 엔티티

| 엔티티 | 설명 |
|---|---|
| `Applicant` | 지원자 계정 |
| `Company` | 기업 계정 (HR 담당자) |
| `Employment` | 지원자-기업 간 하나의 고용 관계. 상태머신의 주체 |
| `ConsentRecord` | 동의 기록. `stage: PRE_APPLY \| POST_TERMINATION`로 구분해 각각 별도 저장 |
| `Feedback` | 퇴직 후 기업이 입력한 역량 점수 (11개 항목, 5점 척도) |
| `SubmissionLink` | 지원자가 생성하는 피드백 열람용 링크 (유효기간, 즉시 만료 가능) |
| `AccessLog` | 제출 링크 열람 기록 (기업명, 일시, linkId) |
| `Dispute` | 이의제기 |
| `Credit` / `Subscription` | 기업 과금 단위 |

---

## 3. 기능 요구사항 — 동의 & 상태머신 (최우선 구현 대상)

### 3.1 Employment 상태 전이

```
APPLIED
 └─ 지원자 사전동의 체크
     ├─ 동의 → CONSENT_PRE_GIVEN
     └─ 미동의 → CONSENT_NONE (이후 영구적으로 피드백 불가)

CONSENT_PRE_GIVEN
 └─ 입사 처리 → EMPLOYED
     ※ EMPLOYED 상태에서는 기업의 피드백 입력 API가 항상 거부되어야 함 (재직 중 입력 원천 차단)

EMPLOYED
 └─ 퇴직 처리 → TERMINATED
     └─ 시스템이 지원자에게 "최종 동의 재확인" 알림 발송 + 14일 타이머 시작

TERMINATED (14일 이내)
 ├─ 지원자 '동의' → CONSENT_FINAL_GIVEN (기업 입력창 활성화 + 30일 타이머 시작)
 ├─ 지원자 '거부' → CONSENT_REJECTED (영구 차단)
 └─ 14일 무응답 → CONSENT_REJECTED (자동, 동일 처리)

CONSENT_FINAL_GIVEN (30일 이내)
 ├─ 기업이 점수 입력 완료 → FEEDBACK_SUBMITTED
 └─ 30일 경과 → FEEDBACK_EXPIRED (영구 차단, 크레딧 환급 없음)
```

### 3.2 요구사항 상세

- **REQ-1**: 동의는 반드시 2단계로 분리 저장한다. `PRE_APPLY`와 `POST_TERMINATION` 동의를 하나의 레코드로 합치지 않는다. (분쟁 시 각 단계 타임스탬프를 개별 증빙으로 제출해야 함)
- **REQ-2**: 재직 중(`EMPLOYED`) 상태에서는 피드백 입력 엔드포인트가 프론트 숨김이 아니라 **API 레벨에서 403**을 반환해야 한다. (`Employment.status !== 'CONSENT_FINAL_GIVEN'`이면 write 거부)
- **REQ-3**: 14일/30일 타이머는 지연 큐 또는 크론 배치로 만료 처리한다. 무응답은 자동으로 `CONSENT_REJECTED`로 전환되어야 한다.
- **REQ-4**: 모든 상태 전이는 감사로그(누가/언제/무슨 동의)로 별도 테이블에 적재한다.

---

## 4. 기능 요구사항 — 피드백 데이터

### 4.1 허용 필드
- 5점 척도 점수, 11개 표준 항목: 업무지시이행도, 납기·기한준수율, 업무결과물품질, 자발적업무참여도,
  협업행동, 의사소통명확성, 갈등상황대응, 직무전문지식, 문제해결능력, 전반적업무기여도, 재고용의향
- 각 항목에 `해당없음` 옵션 필수

### 4.2 금지 사항 (스키마 레벨 제약)
- **REQ-5**: `Feedback` 테이블/스키마에 자유 서술형 텍스트 컬럼을 만들지 않는다 (댓글/사유/코멘트 등 어떤 이름으로도). 프론트 숨김이 아니라 DB 컬럼 자체가 없어야 함 — 이게 명예훼손 리스크를 구조적으로 차단한다는 서비스의 법적 논리의 핵심이므로, 추후 "사유 입력 필드 추가해달라" 요청이 와도 이 REQ 근거로 반려 대상.
- 수집 금지 항목(필드로 만들면 안 됨): 퇴직사유/징계/비위 암시, 자유서술 의견, 외모/건강/장애, 나이/성별/출신/학벌/종교/혼인, 범죄·비위 사실

---

## 5. 기능 요구사항 — 접근 제어 & 유출 방지

- **REQ-6**: `SubmissionLink`는 지원자가 생성하며 유효기간 설정, 즉시 revoke가 가능해야 한다.
- **REQ-7**: 링크를 통한 리포트 조회 시마다 `AccessLog`(기업명, 조회일시, linkId)를 기록하고 지원자 대시보드에 실시간 노출한다.
- **REQ-8**: 프론트에서 화면 캡처 탐지 + 조회자별 동적 워터마크를 적용한다 (백엔드는 조회자 식별 정보를 워터마크 생성 API에 전달).
- **REQ-9**: 이의제기(`Dispute`) 접수 → 14영업일 내 처리 → 허위 판정 시 해당 피드백 블라인드 처리. 반복 위반 기업은 정지 플래그.

---

## 6. 기능 요구사항 — 과금/크레딧 (B2B 메인 매출)

- 기업 구독 플랜: Starter(월 20크레딧) / Pro(60) / Business(150) / Enterprise(무제한, 커스텀)
- **REQ-10**: 리포트 1건 열람 = 크레딧 1 차감. 크레딧 소진 시 초과분은 건당 추가과금(15,000원) 처리.
- **REQ-11**: 크레딧 단독 구매 패키지(5/20/50/100건) 지원, 수량별 할인율 적용.
- **REQ-12**: AI 심층분석/채용매칭/팀구성 리포트는 크레딧과 별도의 유료 애드온으로 과금.
- **REQ-13**: 데이터 리워드 — 기업이 피드백 입력을 완료하면 이벤트 트리거로 0.3 크레딧 자동 환급(연 최대 10크레딧). 이의제기 0건 유지 기업에는 보너스 크레딧 지급.
- 지원자 요금제: Free / Pro(9,900원/월) / Career+(79,000원/년) — 열람 로그 전체 보기, AI 분석, 링크 무제한 등 기능 게이팅.

---

## 7. 기능 요구사항 — AI 분석 (후순위)

- 복수 기업 피드백 가중평균 종합 점수
- 동종 직군 대비 벤치마크(백분위)
- 시계열 성장 추이
- 채용사 인재상 ↔ 지원자 역량 매칭 점수
- 복수 기업 피드백 일관성 기반 리스크 스코어
- **참고**: 기획서 자체가 "Phase 1은 AI 없이 원점수 기반으로 운영"을 명시함. MVP 범위 아님.

---

## 8. 구현 우선순위

| 우선순위 | 범위 |
|---|---|
| P0 | 동의 상태머신(섹션 3), 재직중 입력 차단, 피드백 스키마 제약(섹션 4) |
| P1 | 접근 제어/열람로그/워터마크(섹션 5), 크레딧/구독 과금(섹션 6) |
| P2 | AI 분석 엔진(섹션 7), ATS 연동 |

P0가 사실상 이 서비스의 법적 정당성 그 자체이므로, 다른 기능보다 먼저 정확히 구현되어야 함.
